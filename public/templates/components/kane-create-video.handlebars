<div class="input-group">
  <div class="input-group-btn">
    <button class="btn btn-primary"
    {{action "verifyAndCreate" newTitle}}>
      <i class="glyphicon glyphicon-plus-sign"></i>
    </button>
  </div>
  {{kane-floatlabel-input 
    placeholder="NEW VIDEO TITLE"}}
</div>
